# Runtime dependencies for the repository-root Vercel backend service.
# Keep in sync with backend/requirements.txt (pytest is local/test-only).
fastapi>=0.115,<1
uvicorn>=0.34,<1
httpx>=0.28,<1
pydantic>=2.10,<3
python-dotenv>=1,<2
pyproj>=3.7,<4
shapely>=2.1,<3
PyYAML>=6,<7
